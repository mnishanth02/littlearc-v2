{
  "name": "@littlearc/api",
  "version": "0.0.0",
  "private": true,
  "description": "LittleArc Fastify API",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rm -rf dist coverage",
    "dev": "pnpm build --watch",
    "start": "node dist/index.js",
    "test": "pnpm --filter @littlearc/contracts build && pnpm --filter @littlearc/database build && pnpm --filter @littlearc/observability build && vitest run src",
    "test:postgres": "pnpm --filter @littlearc/crypto build && pnpm --filter @littlearc/contracts build && pnpm --filter @littlearc/database build && pnpm --filter @littlearc/observability build && tsx integration/off-02-household.ts",
    "validate:device:off02": "pnpm --filter @littlearc/crypto build && pnpm --filter @littlearc/contracts build && pnpm --filter @littlearc/database build && pnpm --filter @littlearc/observability build && tsx integration/off-02-household.ts --device",
    "typecheck": "tsc --project tsconfig.json && tsc --project tsconfig.integration.json"
  },
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@fastify/helmet": "13.1.0",
    "@littlearc/auth": "workspace:*",
    "@littlearc/contracts": "workspace:*",
    "@littlearc/crypto": "workspace:*",
    "@littlearc/database": "workspace:*",
    "@littlearc/domain": "workspace:*",
    "@littlearc/observability": "workspace:*",
    "fastify": "5.10.0"
  },
  "devDependencies": {
    "@types/pg": "8.16.0",
    "pg": "8.22.0",
    "tsx": "4.23.1"
  }
}
