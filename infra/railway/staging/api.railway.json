{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @littlearc/api... build",
    "watchPatterns": [
      "/apps/api/**",
      "/packages/contracts/**",
      "/packages/crypto/**",
      "/packages/database/**",
      "/packages/storage/**",
      "/package.json",
      "/pnpm-lock.yaml",
      "/pnpm-workspace.yaml",
      "/turbo.json"
    ]
  },
  "deploy": {
    "drainingSeconds": 15,
    "healthcheckPath": "/health/ready",
    "healthcheckTimeout": 300,
    "overlapSeconds": 30,
    "restartPolicyMaxRetries": 10,
    "restartPolicyType": "ALWAYS",
    "startCommand": "pnpm --filter @littlearc/api start"
  }
}
